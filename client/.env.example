# Backend API URL (server defaults to port 5000 locally)
# Leave empty in local dev to use Vite proxy (/api → localhost:5000)
# Vercel: set in Project Settings → Environment Variables (Production + Preview)
VITE_API_URL=http://localhost:5000
