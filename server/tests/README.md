# Server tests

```
server/tests/
  api/            # HTTP endpoints (Supertest)
```

Run from the `server/` folder:

```bash
npm test
npm run test:watch
```

Register every API in `docs/API-REGISTRY.md`.
